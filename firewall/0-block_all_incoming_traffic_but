# Update package lists and install UFW
sudo apt-get update
sudo apt-get install ufw

# Allow SSH, HTTPS, and HTTP traffic
sudo ufw allow 22/tcp
sudo ufw allow 443/tcp
sudo ufw allow 80/tcp

# Enable UFW
sudo ufw enable

# Check UFW status to verify rules
sudo ufw status

